<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <!-- 蓝牙必选权限 -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <!-- Android 12+ 需显式声明扫描/连接权限 -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" /> <!-- 车载场景可免位置关联 -->
    <!-- 前台位置权限，使用者：蓝牙 -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:name=".XHStoreApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.XHStoreAndroid"
        tools:targetApi="31" >
        <activity
            android:name=".demos.XHExecutorDemoActivity"
            android:exported="false" />
        <activity
            android:name=".demos.data_item.XHDataItemDemoActivity"
            android:exported="false" />
        <activity
            android:name=".demos.XHBannerDemoActivity"
            android:exported="false" />
        <activity
            android:name=".demos.XHRefreshDemoActivity"
            android:exported="false" />
        <activity
            android:name=".demos.XHTabTopDemoActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="false" />
        <activity
            android:name=".demos.XHTabBottomDemoActivity"
            android:exported="false" />
        <activity
            android:name=".demos.XHLogDemoActivity"
            android:exported="false" />
        <activity
            android:name=".GuideActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>